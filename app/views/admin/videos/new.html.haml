%section.admin_add_video
  .container
    .row
      .col-md-10.col-md-offset-1
        = bootstrap_form_for [:admin, @video], layout: :horizontal, label_col: "col-sm-3", control_col: "col-sm-6"  do |f|
          %ul.nav.nav-tabs
            %li
              %a(href="/ui/admin_views_payments") Recent Payments
            %li.active
              = link_to "Add a New Video", new_admin_video_path
          %br
          %fieldset
            = f.text_field :title
            = f.select :category_id, options_for_select( Category.all.map { |category| [category.title, category.id] } ), label: "Category"
            = f.text_area :description
            = f.file_field :large_cover, label: "Large Cover", class: 'form-control'
            = f.file_field :small_cover, label: "Small Cover", class: "form-control"
            = f.text_field :video_url, label: "Video URL"
          %fieldset.actions.form-group
            .col-sm-6.col-md-offset-3  
              = f.submit